<% provide(:title, 'New Reservation Type') %>

<div class="container admin_page">
  <h1>New Reservation Type</h1>

  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <%= form_for(@reservation_type) do |f| %>

        <%= render 'fields', f: f %>

        <%= f.submit "Submit Reservation Type", class: "btn btn-large btn-primary" %>
      <% end %>
    </div>
  </div>
</div>